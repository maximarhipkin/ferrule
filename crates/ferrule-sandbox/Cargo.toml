[package]
name = "ferrule-sandbox"
version = "0.3.0"
edition = "2021"
description = "OS-level sandbox for agent shell commands: Landlock + seccomp on Linux, Seatbelt on macOS"

[dependencies]
dunce = { workspace = true }
serde = { workspace = true }
tracing = { workspace = true }
libc = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
serde_json = { workspace = true }
